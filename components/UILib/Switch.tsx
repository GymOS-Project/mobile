import React from "react";
import { Switch as RNSwitch, SwitchProps } from "react-native-ui-lib";

const Switch = (props: SwitchProps) => {
  return <RNSwitch {...props} />;
};

export default Switch;
